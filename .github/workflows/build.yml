name: Build ZipZap

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller customtkinter pillow pillow_heif google-genai imageio imageio-ffmpeg moviepy docx2pdf watchdog
          
      - name: Build Windows Executable
        run: |
          $CTK_PATH = python -c "import customtkinter, os; print(os.path.dirname(customtkinter.__file__), end='')"
          pyinstaller --noconfirm --onefile --windowed --name "ZipZap" --add-data "$CTK_PATH;customtkinter/" --copy-metadata imageio --copy-metadata docx2pdf --copy-metadata google-genai --copy-metadata pillow-heif main.py
          
      - name: Upload Windows executable
        uses: actions/upload-artifact@v4
        with:
          name: ZipZap-Windows
          path: dist/ZipZap.exe

  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller customtkinter pillow pillow_heif google-genai imageio imageio-ffmpeg moviepy docx2pdf watchdog
          
      - name: Build Mac App
        run: |
          CTK_PATH=$(python -c "import customtkinter, os; print(os.path.dirname(customtkinter.__file__), end='')")
          pyinstaller --noconfirm --windowed --name "ZipZap" --add-data "$CTK_PATH:customtkinter/" --copy-metadata imageio --copy-metadata docx2pdf --copy-metadata google-genai --copy-metadata pillow-heif main.py
          cd dist && zip -r ZipZap-Mac-App.zip ZipZap.app
          
      - name: Upload Mac App
        uses: actions/upload-artifact@v4
        with:
          name: ZipZap-Mac
          path: dist/ZipZap-Mac-App.zip
